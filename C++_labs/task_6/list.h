#ifndef LIST_H
#define LIST_H

#include <list>
#include <string>

void print_list(std::list<std::string> & lst);
std::list<std::string> makeList();
void changeList(std::list<std::string> &lst);

#endif
